<div class="notification @Type" style="display:@(Visible ? "block" : "none")">
    @Message
</div>

@code {
    [Parameter] public string Message { get; set; } = "";
    [Parameter] public string Type { get; set; } = "info"; 
    [Parameter] public bool Visible { get; set; } = false;
}